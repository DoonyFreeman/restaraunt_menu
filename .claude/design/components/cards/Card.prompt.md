Base surface for any boxed content — the foundation for menu and ceremony cards.

```jsx
<Card>…</Card>
<Card padded={false}><img …/></Card>
```

- `hover` (default on) lifts the card 4px and deepens the shadow.
- `padded` toggles the default 16px inset — turn off when the card leads with a full-bleed photo.
